import React from "react";

// If both page + route are present, the route will get handled by default
// Page won't show
const HelloPage = () => {
  return <div>HelloPage</div>;
};

export default HelloPage;
